<h2>
Customer Relationship Management
<br/>
<small>A system to keep track of leads</small>
</h2>

<table class="table" >
    <thead>
      <tr>
        <th></th>
        <th><div class="room"></div><div class="sortable">Name</div></th>
        <th> <div class="room"></div><div class="sortable">Username</div></th>
        <th>Password</th>
        <th> <div class="room"></div><div class="sortable">Updated At</div></th>
        <th>Options</th>
      </tr>
    </thead>
  </tbody>
    

<%= render "formAlt.html", Map.put(assigns, :action, user_path(@conn, :index)) %>
<%= for user <- @users do %>
 <tr>
      <td><%= user.id %></td>
      <td><%= user.name %></td>
      <td><%= user.username %></td>
      <td><%= String.duplicate(".", String.length(user.password)) %></td>
      <td><%= String.slice(to_string(user.updated_at), 11, 8) %>
      <td class="text-right">
        <span><%= link "Show", to: user_path(@conn, :show, user), class: "act" %></span>
        <span><%= link "Edit", to: user_path(@conn, :edit, user), class: "act" %></span>
        <span><%= link "Delete", to: user_path(@conn, :delete, user), method: :delete, class: "act" %></span>
      </td>
    </tr>
<% end %>
  </tbody>
</table>

